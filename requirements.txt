# Requirements for Aegis-Antigravity SRE Zero-Warehouse Agent
# Pinning versions ensures reproducible builds during the hackathon under Fedora.

# Reflex framework - Pure Python reactive UI compiled to Next.js/React.
reflex>=0.5.0

# SDK for OpenAI's raw model interaction and native function calling.
openai>=1.12.0

# HTTP requests library with connection pooling for n8n webhook notifications.
requests>=2.31.0

# PyArrow and Pandas are necessary for offline metadata inspection and Parquet format integration.
pyarrow>=14.0.0
pandas>=2.1.0

# Python Dotenv for sandboxed and local loading of API keys (avoiding hardcoded keys).
python-dotenv>=1.0.0

# Next-generation Google GenAI Unified SDK
google-genai>=0.1.1
